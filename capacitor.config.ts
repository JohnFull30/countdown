import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.johnfull30.countdown',
  appName: 'GenderRevealCountdown',
  webDir: 'build'
};

export default config;
